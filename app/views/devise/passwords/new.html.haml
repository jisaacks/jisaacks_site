.signin-container
  %h2 Reset Password
  = form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f|
    = devise_error_messages!
    %div
      = f.label :email
      = f.email_field :email
    .submit= f.submit "Send instructions", :class => 'btn btn-inverse'
  = render "devise/shared/links"
