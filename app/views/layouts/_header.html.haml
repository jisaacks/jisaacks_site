.header
  .width
    - if current_user
      = link_to "Sign out", destroy_user_session_path, :method => :delete, :class => 'signout'
    - else
      = link_to "Sign In", new_user_session_path, :class => 'signin'
    .logo jisaacks | programming and stuffs
  = render :partial => 'layouts/nav'